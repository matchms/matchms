name: 'Publish conda package'

on:
  release:
    types: [published]

jobs:

  publish:
    name: Publish conda (${{ matrix.python-version }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ['ubuntu-latest', 'macos-latest', 'windows-latest']
        python-version: ['3.7']
    steps:
      - uses: actions/checkout@v2
      - uses: goanpeca/setup-miniconda@v1
        with:
          miniconda-version: 'latest'
          auto-update-conda: true
          python-version: ${{ matrix.python-version }}
          activate-environment: matchms
          auto-activate-base: false
          channels: conda-forge,bioconda,nlesc
      - name: Install required packages
        shell: bash -l {0}
        run: |
          conda install conda-build conda-verify anaconda
      - name: Show conda config
        shell: bash -l {0}
        run: |
          conda info
          conda list
          conda config --show-sources
          conda config --show
      - name: Build the conda package
        shell: bash -l {0}
        run: |
          conda config --set anaconda_upload no
          # mkdir -p ../../conda-tmp ./output
          # conda build -c bioconda -c conda-forge -c nlesc --croot ../../conda-tmp --output-folder ./output .
          mkdir -p ./output
          conda build -c bioconda -c conda-forge -c nlesc --output-folder ./output .
      - name: Push the package to anaconda cloud
        shell: bash -l {0}
        run: |
          anaconda -t ${{ secrets.ANACONDA_TOKEN }} upload -u nlesc --force ./output/noarch/*.tar.bz2
